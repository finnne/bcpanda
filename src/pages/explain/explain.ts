import { Component } from '@angular/core';
// import { HotCodePush } from '@ionic-native/hot-code-push';

@Component({
  selector: 'page-explain',
  templateUrl: 'explain.html',
})



export class ExplainPage {

	constructor(
		// private chcp: HotCodePush,
	){
		// this.chcp.getVersionInfo().then(res => {
		// 	console.log(res)
		// });

		// alert(JSON.stringify(a))

		// this.chcp.requestApplicationUpdate('update your app');
		
	}


}
